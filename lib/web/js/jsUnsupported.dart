/// @nodoc This is the unsupported version.
class JS {
  final String? name;

  const JS([this.name]);
}

/// @nodoc
class _Anonymous {
  const _Anonymous();
}

/// @nodoc
const _Anonymous anonymous = _Anonymous();
